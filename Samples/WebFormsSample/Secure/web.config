<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.web>
    <!-- Anonymous users are denied here; UrlAuthorizationModule must redirect them to loginUrl. -->
    <authorization>
      <deny users="?" />
    </authorization>
  </system.web>
</configuration>
